#include <stdio.h>

int main()
{
  int npeople, budget, nhotels, nweeks, price, beds, i, j, min = -1;
  scanf("%d %d %d %d", &npeople, &budge, &nhotels, &nweeks);
  for(i=0;i<nhotels;i++)
    {
      scanf("%d", &price);
      for(j=0;j<nweeks;j++)
	{
	  scanf("%d", &beds);
	  if(beds >= npeople && 
	}
    }
}
